[project]
name = "sudno"
version = "0.1.0"
description = "A config driven invocator"
readme = "README.md"
authors = [
    { name = "Shromann Majumder" }
]
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
